[project]
name = "methylation-latent-methylprep-runtime"
version = "0.1.0"
description = "Pinned raw-IDAT preprocessing runtime."
requires-python = ">=3.10,<3.11"
dependencies = [
    "methylprep==1.7.1",
    "numpy==1.24.4",
    "pandas==1.5.3",
]

[tool.uv]
package = false
